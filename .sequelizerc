// eslint-disable-next-line @typescript-eslint/no-var-requires
const { resolve } = require('path');

module.exports = {
  config: resolve('webdriver-server', 'src', 'database', 'cli.js'),
  'models-path': resolve('webdriver-server', 'src', 'models'),
  'seeders-path': resolve('webdriver-server', 'src', 'database', 'seeders'),
  'migrations-path': resolve('webdriver-server', 'src', 'database', 'migrations'),
}